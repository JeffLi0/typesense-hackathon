{
  "name": "typesense-hackathon",
  "version": "1.0.0",
  "description": "Symptom -> condition -> medication cost search. React + Vite frontend, FastAPI + Typesense backend.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "api": "./.venv/bin/uvicorn api:app --app-dir server --host 127.0.0.1 --port 8000 --reload",
    "typesense": "./typesense/typesense-server --data-dir=./typesense/data --api-key=xyz --enable-cors",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
